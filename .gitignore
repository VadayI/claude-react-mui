# Local Claude settings (secrets, tokens) — do NOT commit
.claude/settings.local.json

# Node
node_modules/
.npm/
*.log
npm-debug.log*

# Build output
dist/
dist-ssr/
.vite/

# Test / coverage
coverage/
playwright-report/
test-results/
.playwright/

# Env / secrets
.env
.env.*
!.env.example

# Editor / OS
.idea/
.vscode/
.DS_Store

# Local-only — NOT tracked (downloaded reference, personal notes)
/LOCAL/
/HANDOFF.md

# Claude session-local state — regenerated by the SessionStart hook and /command logging.
# Must not be shared across machines or cloned into derived projects.
.claude/memory/env-detect.json
.claude/memory/command-log.jsonl

# WSL/FUSE artifacts
*.fuse_hidden*
.__wtest
