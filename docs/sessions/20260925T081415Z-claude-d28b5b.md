---
session: 20260925T081415Z-claude-d28b5b
agent: claude
recorded: 2026-09-25T08:14:15Z
branch: feat/p07-shared-memory
revision: f197a310f31f404739a602552650dec2f52bf9cd
---

# Session 20260925T081415Z-claude-d28b5b

<!-- Fill every section before committing. Record facts with their source; never
paste transcripts, env values, tokens or raw logs. -->

## Task

Close the P07 leftovers in React: the shared language preference instead of a CLAUDE.md import, and the bootstrap statement "legacy migration is P07".

## Changes

- Commit `f197a31`: development pin `f8e3162`. AGENTS gives the language
  order (session, then the shared file, then legacy until migrated); bootstrap
  step 0 uses `project_state --language`. `/set-language` writes only
  `docs/ai/overrides/output-language.md`: it no longer edits CLAUDE.md or
  creates `.claude/rules/output-language.md`.
- This record and the `docs/HANDOFF.md` entry.

## Decisions

- Shared preference `docs/ai/overrides/output-language.md`; the legacy
  `.claude/rules/output-language.md` becomes a fixed pointer, so exactly one
  writable preference remains, and old CLAUDE.md imports keep working.
- A conflict is never resolved by the tool alone: the user's current choice
  goes into the shared file, then `--language --apply --keep-shared` runs.
- The start context shows the effective language to every agent. An unmigrated
  legacy file is not a finding; a conflict is.

## Checks

- `python3.13 scripts/ai/core_sync.py --check` — PASS (no drift)
- `python3.13 scripts/ai/generate_adapters.py --check` — PASS
- `python3.13 scripts/ai/generate.py --check` — PASS (53 adapters/packs/manifest)
- `cd scripts/ai && python3.13 -m unittest discover -s . -p "test_*.py"` — 34 OK
- Cloud Linux container, Python 3.13.13, Node 22.22.2, Git 2.43.0. Candidate `f197a31`, base `4799db0`.

## Limitations

- Windows-native runs of `project_state --language` and `session_context.py`: NOT_VERIFIED
  (Linux fixtures cover CRLF/BOM and a cp1252 stream).
- The runtime acceptance run (Claude → Codex → Claude, parallel sessions) is written
  down but NOT_VERIFIED until someone executes it.
- Hosted CI for these commits: pending the push. The core pin is a development pin.

## Next step

After contract #66 is merged on the user's command: repin to the integrated core and restore «Tymczasowy pin deweloperski»; then #76 → #77. The runtime acceptance run follows docs/ai/session-continuity.md.
