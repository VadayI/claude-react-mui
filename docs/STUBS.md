# Stubs Ledger

This project has no outstanding stubs. Every `// STUB:` marker in `src/` must have an entry here before merging to `main`.

| File:line | Reason | Test that forces real impl | Owner | Date |
| --------- | ------ | -------------------------- | ----- | ---- |

<!-- Add rows here when stubs are introduced. Remove rows when stubs are resolved. -->
