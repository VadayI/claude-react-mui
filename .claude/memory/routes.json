[
  {
    "path": "/articles",
    "feature": "articles",
    "screen": "ArticlesPage",
    "auth": "authenticated",
    "states": ["loading", "success", "empty", "error"],
    "consumes": ["GET /api/v1/articles", "POST /api/v1/articles"],
    "notes": "Article list with create form. Auth via Bearer/JWT (middleware). No /login route — auth is API-layer only."
  }
]
