#!/usr/bin/env sh
# Verify every Git pre-push ref update against an exact candidate/base.
set -e
PYTHON="${AI_PYTHON:-python}"
"$PYTHON" -c 'import sys; sys.exit(0 if sys.version_info >= (3, 13) else 2)'
"$PYTHON" scripts/ai/git_hooks.py pre-push --remote "$1"
