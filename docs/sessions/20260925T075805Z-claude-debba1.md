---
session: 20260925T075805Z-claude-debba1
agent: claude
recorded: 2026-09-25T07:58:05Z
branch: feat/p07-shared-memory
revision: a8c769a27913986b869bb3a6caa884f9e28d23ad
---

# Session 20260925T075805Z-claude-debba1

<!-- Fill every section before committing. Record facts with their source; never
paste transcripts, env values, tokens or raw logs. -->

## Task

P07 session continuity in the React template: shared start context in SessionStart, session records at wrap-up, and removal of stale P07 statements in AGENTS.md.

## Changes

- Commit `a8c769a` (`feat/p07-shared-memory`): development pin to contract
  `6fb703a`; the new core files are in `seed-inputs.json`; `scripts/session-start.mjs`
  prints `session_context.py`; `ci_mode.py` DOCUMENTATION map; `generate.py`
  rejects `docs/sessions/` seeds; AGENTS (start step, the shared-memory map,
  no stale "until P07"), the neutral wrap-up, git-operations, living-plan,
  orchestrate, docs-writer, `/handoff`, `/audit` and `docs/ai/README` are updated;
  adapters, role packs and the delivery manifest are regenerated.
- This record and the `docs/HANDOFF.md` entry.

## Decisions

- `docs/project-state/project.json` → `documentation` is authoritative; defaults
  cover projects without a map. Entries may only name documents or directories
  (no hidden, secret or configuration files).
- One record per session in `docs/sessions/` (`<UTC>-<agent>-<hex6>.md`, exclusive
  create); `docs/WORKLOG.md` stays as history; no `merge=union`.
- The newest record present in the working tree is the latest (merged records
  count). Relevant commits after it are a `--check` finding; a recorded revision
  missing from a clone (squash/rebase, shallow) is information only.

## Checks

- `python3.13 scripts/ai/core_sync.py --check` — PASS (no drift)
- `python3.13 scripts/ai/generate_adapters.py --check` — PASS
- `python3.13 scripts/ai/generate.py --check` — PASS (53 adapters/packs/manifest)
- `cd scripts/ai && python3.13 -m unittest discover -s . -p "test_*.py"` — 34 OK
- `AI_PYTHON=python3.13 node scripts/session-start.mjs` — exit 0, context printed
- Cloud Linux container, Python 3.13.13, Node 22.22.2, Git 2.43.0. Candidate `a8c769a`, base `f7a41e7`.

## Limitations

- Windows-native run of `session_context.py` and the SessionStart hooks: NOT_VERIFIED
  (Linux only; the cp1252 stdout case is covered by a fixture).
- A real Claude → Codex → Claude session sequence on another machine: NOT_VERIFIED;
  only the mechanical fixtures ran.
- Hosted CI: not run — the branch is not pushed yet. The core pin is a development pin.

## Next step

After contract #66 is merged on the user's command: repin to the integrated core, restore the assertions marked «Tymczasowy pin deweloperski» in `scripts/ai/test_family_delivery.py`, then merge #76 → #77. After that, P08.
